@* @model IEnumerable<BookStore.Model.Book>
@using BookStore.Model
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <title>Book Nexus</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        html, body {
            height: 100%;
        }

        body {
            display: flex;
            flex-direction: column;
            background-color: #f5f5f5;
        }

        .layOne {
            flex: 1 0 auto;
        }

        /* Header */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            background-color: #4A3F35;
            color: white;
            position: relative;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
        }

        nav ul {
            list-style: none;
            display: flex;
        }

            nav ul li {
                margin: 0 15px;
            }

                nav ul li a {
                    color: white;
                    text-decoration: none;
                    font-size: 18px;
                }

        .search-bar {
            display: flex;
            margin-left: 700px;
        }

            .search-bar input {
                padding: 5px;
                border: none;
                border-radius: 5px;
            }

            .search-bar button {
                margin-left: 5px;
                padding: 5px 10px;
                border: none;
                background-color: #E7A977;
                color: white;
                border-radius: 5px;
                cursor: pointer;
            }

        /* Main content */
        main {
            flex: 1;
            padding: 20px;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            background-color: #4A3F35;
            color: white;
            flex-shrink: 0;
        }

        .footer-content {
            max-width: 100%;
            margin: auto;
        }

        .social-links {
            list-style: none;
            display: flex;
            justify-content: center;
            margin-top: 10px;
        }

            .social-links li {
                margin: 0 10px;
            }

            .social-links a {
                color: white;
                text-decoration: none;
            }

        .login-btn {
            text-decoration: none;
            background-color: #E7A977;
            color: white;
            padding: 8px 15px;
            border-radius: 5px;
            font-size: 16px;
            transition: 0.3s;
            position: relative;
            margin-left: 10px;
        }

            .login-btn:hover {
                background-color: #D18A5B;
            }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        .books-header {
            text-align: center;
            margin: 30px 0;
            color: #4A3F35;
        }

        .books-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 30px;
            padding: 20px;
        }

        .book-card {
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

            .book-card:hover {
                transform: translateY(-5px);
            }

        .book-image {
            width: 100%;
            height: 300px;
            object-fit: unset; /* Ensures no stretching */
        }

        .book-details {
            padding: 15px;
        }

        .book-title {
            font-size: 1.2rem;
            margin-bottom: 8px;
            color: #4A3F35;
        }

        .book-author {
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 5px;
        }

        .book-meta {
            font-size: 0.8rem;
            color: #888;
            margin-bottom: 10px;
        }

        .book-description {
            font-size: 0.9rem;
            color: #555;
            margin-top: 10px;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .action-buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
        }

        .btn {
            padding: 8px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9rem;
        }

        .btn-view {
            background-color: #4A3F35;
            color: white;
        }

        .btn-download {
            background-color: #E7A977;
            color: white;
        }

        /* PDF Viewer Styles */
        .pdf-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.8);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }

        .pdf-container {
            background: white;
            width: 90%;
            height: 90%;
            padding: 20px;
            border-radius: 8px;
            position: relative;
        }

        .pdf-viewer {
            width: 100%;
            height: calc(100% - 50px);
            border: none;
        }

        .close-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            font-size: 24px;
            background: none;
            border: none;
            cursor: pointer;
            color: #333;
        }

        .pdf-actions {
            text-align: center;
            margin-top: 10px;
        }

        @@media (max-width: 768px) {
            header {
                flex-direction: column;
                align-items: flex-start;
                padding: 10px 20px;
            }

            .logo {
                font-size: 20px;
                margin-bottom: 10px;
            }

            nav ul {
                flex-direction: column;
                width: 100%;
                gap: 10px;
            }
            /* New container for search and login */
            .header-actions {
                display: flex;
                width: 100%;
                gap: 10px;
                align-items: center;
                margin-top: 10px;
            }

            .search-bar {
                display: flex;
                flex: 1;
                gap: 10px;
            }

                .search-bar input {
                    flex: 1;
                    min-width: 0;
                }

                .search-bar button {
                    white-space: nowrap;
                    flex-shrink: 0;
                    padding: 5px 12px;
                }

            .login-btn {
                white-space: nowrap;
                padding: 5px 12px;
            }

            main {
                padding: 15px;
            }

            footer {
                padding: 15px;
                font-size: 14px;
            }
        }

    </style>
</head>
<body class="layOne">
    <header>
        <div class="logo">📚 BookNexus</div>
        <div class="search-bar">
            <input type="text" onkeyup="handleSearch()" id="search" placeholder="Search books...">
        </div>
        <a id="lgn" href="/Login" class="login-btn">Login</a>
    </header>

    
    <main>

        <div id="pdfOverlay" class="pdf-overlay">
            <div class="pdf-container">
                <button class="close-btn" onclick="closePdfViewer()">×</button>
                <iframe id="pdfViewer" class="pdf-viewer"></iframe>
                <div class="pdf-actions">
                    <a id="pdfDownloadLink" class="btn btn-download" download>Download PDF</a>
                </div>
            </div>
        </div>

        <h1 class="books-header">Our Book Collection</h1>

        <div class="books-grid">
            @foreach (var book in Model)
            {
                <div class="book-card" id="@book._id">
                    <img src="@book.imgPath" alt="@book.Title" class="book-image">
                    <div class="book-details">
                        <h3 class="book-title">@book.Title</h3>
                        <p class="book-author">By @book.Author</p>
                        <p class="book-meta">Published by @book.Publisher in @book.Year</p>
                        <p class="book-description">@book.Description</p>
                        <div class="action-buttons">
                            @if (Context.Request.Cookies["jwtToken"] != null)
                            {
                                <button class="btn btn-download"
                                        data-book-path="@book.path"
                                        onclick="fetchAndDisplayPdf('@book.path')">
                                    Read and Download
                                </button>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
        @RenderBody()
    </main>

    <footer>
        <div class="footer-content">
            <p>&copy; 2025 BookNexus. All Rights Reserved.</p>
            <ul class="social-links">
                <li><a href="#">📘 Facebook</a></li>
                <li><a href="#">🐦 Twitter</a></li>
                <li><a href="#">📸 Instagram</a></li>
            </ul>
        </div>
    </footer>
    <script src="~/js/major.js" asp-append-version="true"></script>
</body>
</html> *@

@model IEnumerable<BookStore.Model.Book>
@using BookStore.Model
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <title>Book Nexus</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        html, body {
            height: 100%;
        }

        body {
            display: flex;
            flex-direction: column;
            background-color: #f5f5f5;
        }

        .layOne {
            flex: 1 0 auto;
        }

        /* Header */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            background-color: #4A3F35;
            color: white;
            position: relative;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
        }

        nav ul {
            list-style: none;
            display: flex;
        }

            nav ul li {
                margin: 0 15px;
            }

                nav ul li a {
                    color: white;
                    text-decoration: none;
                    font-size: 18px;
                }

        .header-right {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .search-bar {
            display: flex;
            align-items: center;
        }

            .search-bar input {
                padding: 8px 12px;
                border: none;
                border-radius: 5px 0 0 5px;
                width: 250px;
            }

            .search-bar button {
                padding: 8px 15px;
                border: none;
                background-color: #E7A977;
                color: white;
                border-radius: 0 5px 5px 0;
                cursor: pointer;
            }

        .login-btn {
            text-decoration: none;
            background-color: #E7A977;
            color: white;
            padding: 8px 15px;
            border-radius: 5px;
            font-size: 16px;
            transition: 0.3s;
            margin-left:30px;
        }

            .login-btn:hover {
                background-color: #D18A5B;
            }

        /* Main content */
        main {
            flex: 1;
            padding: 20px;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 20px;
            background-color: #4A3F35;
            color: white;
            flex-shrink: 0;
        }

        .footer-content {
            max-width: 100%;
            margin: auto;
        }

        .social-links {
            list-style: none;
            display: flex;
            justify-content: center;
            margin-top: 10px;
        }

            .social-links li {
                margin: 0 10px;
            }

            .social-links a {
                color: white;
                text-decoration: none;
            }

        .books-header {
            text-align: center;
            margin: 30px 0;
            color: #4A3F35;
        }

        .books-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 30px;
            padding: 20px;
        }

        .book-card {
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

            .book-card:hover {
                transform: translateY(-5px);
            }

        .book-image {
            width: 100%;
            height: 300px;
            object-fit: unset;
        }

        .book-details {
            padding: 15px;
        }

        .book-title {
            font-size: 1.2rem;
            margin-bottom: 8px;
            color: #4A3F35;
        }

        .book-author {
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 5px;
        }

        .book-meta {
            font-size: 0.8rem;
            color: #888;
            margin-bottom: 10px;
        }

        .book-description {
            font-size: 0.9rem;
            color: #555;
            margin-top: 10px;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .action-buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
        }

        .btn {
            padding: 8px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9rem;
        }

        .btn-view {
            background-color: #4A3F35;
            color: white;
        }

        .btn-download {
            background-color: #E7A977;
            color: white;
        }

        /* PDF Viewer Styles */
        .pdf-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.8);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .pdf-container {
            background: white;
            width: 90%;
            max-width: 900px;
            height: 90%;
            max-height: 800px;
            border-radius: 8px;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        .close-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            color: #333;
            z-index: 10;
        }

        .pdf-viewer-container {
            flex: 1;
            overflow: auto;
            padding: 20px;
        }

        #pdfCanvas {
            margin: 0 auto;
            display: block;
            max-width: 100%;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .pdf-controls {
            padding: 10px;
            background: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
        }

            .pdf-controls button {
                padding: 5px 15px;
                background: #333;
                color: white;
                border: none;
                border-radius: 4px;
                cursor: pointer;
            }

        .pdf-actions {
            padding: 15px;
            text-align: center;
            background: #f5f5f5;
            border-top: 1px solid #ddd;
        }

        .btn-download {
            padding: 8px 20px;
            background: #4CAF50;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            display: inline-block;
        }

        .loading {
            color: white;
            font-size: 18px;
        }

        .error-message {
            color: #721c24;
            background-color: #f8d7da;
            border-color: #f5c6cb;
            padding: 20px;
            border-radius: 4px;
            margin: 20px;
        }

        @@media (max-width: 768px) {

            header {
                flex-direction: column;
                align-items: flex-start;
                padding: 10px 20px;
            }

            .logo {
                font-size: 20px;
                margin-bottom: 10px;
            }

            nav ul {
                flex-direction: column;
                width: 100%;
                gap: 10px;
            }

            .header-right {
                width: 100%;
                flex-direction: column;
                gap: 10px;
                margin-top: 10px;
            }

            .search-bar {
                width: 100%;
            }

                .search-bar input {
                    width: 100%;
                    border-radius: 5px;
                }

                .search-bar button {
                    border-radius: 5px;
                }

            .login-btn {
                width: 30%;
                text-align: center;
                margin-left: 10px;
            }
        }

        @@media (min-width: 769px) {
            .header-right {
                display: flex;
                align-items: center;
                gap: 15px;
            }
        }
    </style>
</head>
<body class="layOne">
    <header>
        <div class="logo">📚 BookNexus</div>
        <div class="header-right">
            <div class="search-bar">
                <input type="text" onkeyup="handleSearch()" id="search" placeholder="Search books...">
                <a id="lgn" href="/Login" class="login-btn">Login</a>
            </div>
            
        </div>
    </header>

    <main>
        <div id="pdfOverlay" class="pdf-overlay">
            <div class="pdf-container">
                <button class="close-btn" onclick="closePdfViewer()">×</button>
                <iframe id="pdfViewer" class="pdf-viewer"></iframe>
                <div class="pdf-actions">
                    <a id="pdfDownloadLink" class="btn btn-download" download>Download PDF</a>
                </div>
            </div>
        </div>

        <h1 class="books-header">Our Book Collection</h1>

        <div class="books-grid">
            @foreach (var book in Model)
            {
                <div class="book-card" id="@book._id">
                    <img src="@book.imgPath" alt="@book.Title" class="book-image">
                    <div class="book-details">
                        <h3 class="book-title">@book.Title</h3>
                        <p class="book-author">By @book.Author</p>
                        <p class="book-meta">Published by @book.Publisher in @book.Year</p>
                        <p class="book-description">@book.Description</p>
                        <div class="action-buttons">
                            @if (Context.Request.Cookies["jwtToken"] != null)
                            {
                                <button class="btn btn-download"
                                        data-book-path="@book.path"
                                        onclick="fetchAndDisplayPdf('@book.path')">
                                    Read and Download
                                </button>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
        @RenderBody()
    </main>

    <footer>
        <div class="footer-content">
            <p>&copy; 2025 BookNexus. All Rights Reserved.</p>
            <ul class="social-links">
                <li><a href="#">📘 Facebook</a></li>
                <li><a href="#">🐦 Twitter</a></li>
                <li><a href="#">📸 Instagram</a></li>
            </ul>
        </div>
    </footer>
    <div id="cookieConsent" style="display: none; position: fixed; bottom: 0; background: #333; color: white; padding: 10px; width: 100%; text-align: center;">
        <p>We use cookies for authentication. Do you accept?</p>
        <button id="acceptCookies">Accept</button>
        <button id="declineCookies">Decline</button>
    </div>
    <script src="~/js/major.js" asp-append-version="true"></script>
</body>
</html>